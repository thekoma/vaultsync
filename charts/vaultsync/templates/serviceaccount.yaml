{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vaultsync.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vaultsync.labels" . | nindent 4 }}
{{- end }}
